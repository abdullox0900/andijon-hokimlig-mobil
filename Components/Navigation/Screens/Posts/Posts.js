import * as React from 'react';
import { View, Text, Image } from 'react-native';
import { PostNavigatin } from '../../Navigation';

export default function PostInner({ navigation }) {

    return (
        <PostNavigatin />
    );
}